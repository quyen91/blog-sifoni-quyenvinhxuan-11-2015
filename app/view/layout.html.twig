<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>sifony</title>
	<link rel="stylesheet" type="text/css" href="/css/style.css">
	 <link href="/css/bootstrap.min.css" rel="stylesheet">
	 <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.3/jquery.min.js"></script>
</head>
<body>
	<div class="main-contain">
		<header id="header" class="wrap">
			<div class="top-header">
				<div class="logo-header">
					<img src="/img/logo2.png" alt="" height="200" width="200">
				</div>
				<div class="account">
					<ul>
					{% if app.session.has('logged')%}
						
							<li> <a href="{{url('loadlogin')}}" style="color:#ff0000"> {{app.session.get('logged')}} </a></li>
							<li><a href="{{ url('loggout') }}"> Loggout </a></li>

					{% else %}
						<li> <a href="{{url('loadlogin')}}">Login | Signup</a></li>
					{% endif %}

					<li> <a href="{{url('viewCart')}}"> Cart </a> ( <span id="countcart"> 
						{% if app.session.get('cart') == null %} {{0}}{% else %}
							{{ app.session.get('cart') | length }}
						 {% endif %}
						</span> ) </li> 
						<li> <a href=""> About</a></li>
					
						</ul>
				</div>
			</div>
			<div class="bottom-header">
				<nav class="my-nav">
					<ul>
						<li><a href="{{url('home')}}" title="">Home</a></li>
						<li><a href="{{url('mainpage_route')}}" title="">Sản phẩm</a></li>
						<li><a href="{{url('newspage')}}" title="">Tin tức</a></li>
						<li><a href="#" title="">Liên hệ</a></li>
					</ul>
				</nav>
			</div>
			
		</header><!-- /header -->
		<div class="maincontent">
			{% block maincontent %}


			{% endblock %}
		</div>

		<footer>
			
		</footer>
	</div>
	
</body>
</html>